# Copy this file to `.env` and fill in your published-to-web CSV URLs.
# `.env` is gitignored, so the real URLs never get committed.
# build.py loads `.env` automatically for local builds.
CSV_URL_TEAMS=your-teams-csv-url
CSV_URL_MATCHES=your-matches-csv-url
